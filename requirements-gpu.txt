# Optional: exact NVIDIA CUDA library pins for reproducible GPU training on
# Linux x86_64. These wheels don't exist for macOS or Windows, which is why
# they were moved out of requirements.txt (they made `pip install` fail
# outright on any non-matching platform, even for CPU-only use).
#
# Install with:
#   pip install -r requirements.txt -r requirements-gpu.txt
#
# On a fresh setup you can usually skip this file entirely and instead run:
#   pip install "tensorflow[and-cuda]==2.15.0.post1"
# which resolves compatible CUDA wheels for you.
nvidia-cublas-cu12==12.2.5.6
nvidia-cuda-cupti-cu12==12.2.142
nvidia-cuda-nvcc-cu12==12.2.140
nvidia-cuda-nvrtc-cu12==12.2.140
nvidia-cuda-runtime-cu12==12.2.140
nvidia-cudnn-cu12==8.9.4.25
nvidia-cufft-cu12==11.0.8.103
nvidia-curand-cu12==10.3.3.141
nvidia-cusolver-cu12==11.5.2.141
nvidia-cusparse-cu12==12.1.2.141
nvidia-nccl-cu12==2.16.5
nvidia-nvjitlink-cu12==12.2.140
